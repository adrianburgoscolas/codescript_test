import { HellofragmentDirective } from './hellofragment.directive';

describe('HellofragmentDirective', () => {
  it('should create an instance', () => {
    const directive = new HellofragmentDirective();
    expect(directive).toBeTruthy();
  });
});
